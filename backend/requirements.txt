# Solven backend — exact pins from the tested venv (2026-08-14).
# Reproducible installs: `pip install -r requirements.txt` in CI/deploy must
# match the versions that passed the test suite. Bump deliberately + re-test.
fastapi==0.141.1
uvicorn==0.52.1
pydantic==2.13.4
pydantic-settings==2.15.0
langgraph==1.2.11
httpx==0.28.1
pytest==9.1.1
psycopg[binary]==3.3.4
